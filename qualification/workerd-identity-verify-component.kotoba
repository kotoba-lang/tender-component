(ns qualification.workerd-identity-verify
  (:capabilities #{:identity/verify}))

(defn main []
  (bool-result
   (typed-cap-call
    :identity/verify
    [:record :cap/bytes-request [[:bytes :string]]]
    :bool
    (record [:record :cap/bytes-request [[:bytes :string]]] "signed"))))
